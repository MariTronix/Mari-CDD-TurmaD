#Triangulo
resp = 's'

while resp == 's' or resp == 'S':
    base = float(input("Valor da base: "))
    altura = float(input("Valor da altura: "))
    area = (base * altura) / 2
    print(area)
    resp = input("Deseja calcula de novo(S/N): ")

## Quadrado

resp = 's'

while resp == 's' or resp == 'S':
    base = float(input("Valor da base: "))
    altura = float(input("Valor da altura: "))
    area = (base * altura) / 2
    print(area)
    resp = input("Deseja calcula de novo(S/N): ")